{
  "name": "listlead",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PATH=$HOME/.bun/bin:$PATH concurrently -k -n api,web -c blue,green \"cd backend && bun run dev\" \"sleep 3 && cd frontend && bun run dev\"",
    "install:all": "cd backend && bun install && cd ../frontend && bun install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}